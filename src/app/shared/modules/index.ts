import { CommonModule } from '@angular/common';
import { RouterModule } from '@angular/router';
import { MaterialModule } from './material';

export const modules: any[] = [CommonModule, RouterModule, MaterialModule];
