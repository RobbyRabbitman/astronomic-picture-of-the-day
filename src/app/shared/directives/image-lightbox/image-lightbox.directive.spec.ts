import { ImageLightboxDirective } from './image-lightbox.directive';

describe('ImageLightboxDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageLightboxDirective();
    expect(directive).toBeTruthy();
  });
});
